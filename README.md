# Single-Port-Synchronous-SRAM
This project implements a Single-Port Synchronous SRAM using Verilog. The design allows controlled memory access where read and write operations are synchronized with a clock signal, ensuring stable and predictable behavior.
